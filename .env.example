# Frontend (optional — defaults to /api on same domain)
VITE_API_URL=/api

# Backend — required on Vercel (Settings → Environment Variables)
# MONGO_URI=mongodb+srv://<user>:<password>@<cluster>.mongodb.net/user_management?appName=task
